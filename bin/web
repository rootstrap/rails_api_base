#!/usr/bin/env ruby

require 'shellwords'
require_relative 'helpers'

command = if running_with_docker?
  puts "Running in service #{web_service}"

  "docker-compose exec #{web_service} sh -c "
else
  ''
end

exec("#{command}#{Shellwords.join(ARGV)}")
