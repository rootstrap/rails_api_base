#!/usr/bin/env ruby

require 'shellwords'
require_relative 'helpers'

command = if running_with_docker?
  web_service_running? ? "docker-compose exec #{web_service} sh -c " : ''
end

exec("#{command}#{Shellwords.join(ARGV)}")
